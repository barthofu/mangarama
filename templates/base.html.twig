<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('build/app.css') }}">
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>

        <nav class="navbar">
            <div class="left">
                <a href="{{ path('index') }}"><h1>Mangarama</div></a>
            </div>
            <div class="right">
                <a href="{{ path('mangaAdd') }}"><i class="fas fa-plus"></i></a>
                <a href="{{ path('mangaBulkImport') }}"><i class="fas fa-file-import"></i></a>
                <a href="{{ path('stats') }}"><i class="fas fa-chart-pie"></i></a>
                <a href="{{ path('index') }}">À propos</a>
            </div>
        </nav>

        {% block body %}{% endblock %}

        <script src="https://kit.fontawesome.com/d055a26e11.js"></script>
    </body>
</html>